version: '3.8'
services:
  web:
    build: .
    container_name: crytocsv_web
    ports:
      - "5000:5000"
    volumes:
      - ./downloads:/app/downloads
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
